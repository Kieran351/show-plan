---
name: show-plan
description: 手动展示编码前的改动文件、位置及 before / after。
disable-model-invocation: true
---

根据当前需求只读检查代码；输出计划后停止，等待用户审阅，不修改文件。

- 按文件、位置分组，以当前行号和代码锚点定位；插入注明前/后。
- before 原样摘录当前工作区代码；after 给出同一范围的拟改代码，不用省略号或伪代码。
- 新增 before 写“（无）”，删除 after 写“（删除）”；新文件位置写“新文件”，after 给完整内容。
- 信息不足仅澄清或说明阻塞；无需改动仅输出“无代码改动。”

仅按以下格式输出实际内容，代码块指定对应语言，说明使用简体中文。

````markdown
改动文件：[文件路径](/绝对路径:行号)
改动位置：当前行号及代码锚点

before
```ts
当前代码
```

after
```ts
拟改代码
```
````
